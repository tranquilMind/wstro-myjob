<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wstro.dao.echat.TsBaseUserDao">

   <select id="queryList" resultType="ChatBaseUser">
		select * from echat_user m where m.delete_flag =0 
		<if test="null!=userId">
			and m.user_id = #{userId}
		</if>
		<if test="null!=userFriendId">
			and m.user_friend_id = #{userFriendId}
		</if>
		<if test="null!=userFriendGroupId">
			and m.user_friend_group_id = #{userFriendGroupId}
		</if>
		<if test="null!=userGroupId">
			and m.user_group_id = #{userGroupId}
		</if>
	</select>
	
</mapper>